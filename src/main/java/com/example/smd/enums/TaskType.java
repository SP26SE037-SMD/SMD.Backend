package com.example.smd.enums;

public enum TaskType {
    SYLLABUS,        // Nhiệm vụ liên quan đến giáo trình
    CURRICULUM,      // Nhiệm vụ liên quan đến chương trình học
    SUBJECT,         // Nhiệm vụ liên quan đến môn học
    DOCUMENT         // Nhiệm vụ liên quan đến tài liệu
}
